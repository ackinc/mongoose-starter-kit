This repo becomes the "models" folder in new projects.

See user.js for an example schema file. Copy it for new schemas.

To import all models into your main script, do `require('path/to/models/folder')`

Dependencies: mongoose, validator, mocha, chai

